package pl.edu.pw.elka.stud.tkogut.passim.app

import pl.edu.pw.elka.stud.tkogut.agentplatform.{Agent, AgentManagmentPlatform}
import pl.edu.pw.elka.stud.tkogut.passim.{PassimBroker, PassimTaskManager, PassimDialect}


/**
 * User:  Tomasz Kogut
 * Email: tomasz.kogut@hotmail.com
 * Date:  20.06.12
 * Time:  17:35
 */

object ApplicationEntry {
/*
  def main(args: Array[String]) {
    configureLogging()
    AgentManagmentPlatform.startPlatform( List[Agent](
      new PassimBroker,
      new GoogleSearcher,
      new BingSearcherAgent,
      new CiteSeerSearcherAgent,
      new MongoDblpSearcher,
      new ArnetMinerSearcher,
      new DummyClient
    ));
  }
  */

  /*
  def configureLogging() {
    Logging.configure {
      log =>
        log.registerWithJMX = true
        log.level = Level.DEBUG
//        log.loggers("com.myproject.weebits") = Level.OFF

        log.console.enabled = true
        log.console.threshold = Level.DEBUG

        log.file.enabled = true
        log.file.filename = "passimApp.log"
        log.file.maxSize = 10 * 1024 // KB
        log.file.retainedFiles = 5 // keep five old logs around
        // syslog integration is always via a network socket
    }
  }
  */

}
